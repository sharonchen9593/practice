/**
 * Given a binary tree with each node as integers (positive and negative). Count the number of
 * paths that sum to a given value. The path does not need to start or end at the root or a leaf
 * but must go downwards (from parent to child).
 *
 * Hints: 6, 14, 52, 68, 77, 87, 94, 103, 108, 115
 *
 * Time Complexity:
 * Space Complexity:
 **/